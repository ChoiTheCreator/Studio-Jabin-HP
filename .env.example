# Public site URL used for metadata and canonical links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional backend destination for inquiry payloads.
# When omitted, inquiries are accepted and logged with sensitive fields redacted.
# Before enabling it in production, document the processor in the privacy notice.
INQUIRY_WEBHOOK_URL=
