# Jabin 시각 품질 하네스

이 문서는 UI를 수정할 때 섹션 간 통일감, 여백, 정렬을 검증하는 필수 기준이다. `DESIGN.md`의 시각 언어를 구현 단계에서 수치와 화면으로 확인하기 위해 사용한다.

## 1. 섹션 여백 기준

섹션 경계부터 첫 번째 의미 있는 콘텐츠까지의 세로 거리를 확인한다. 첫 콘텐츠는 eyebrow, 제목, 이미지 중 실제로 가장 먼저 보이는 요소다.

| 뷰포트 | 권장 범위 | 허용 상한 |
| ------ | --------- | --------- |
| 390px  | 72~96px   | 112px     |
| 768px  | 88~112px  | 128px     |
| 1440px | 96~144px  | 160px     |

- 같은 위계의 인접 섹션은 첫 콘텐츠 시작 위치의 차이가 32px을 넘지 않아야 한다.
- 높이만 차지하는 빈 wrapper, 고정 `min-height`, 임의의 큰 `padding-top`으로 장면을 만들지 않는다.
- 의미 있는 시각 요소 없이 `240px` 또는 뷰포트 높이의 `28%`보다 큰 빈 영역이 생기면 결함으로 처리한다.
- Hero, 전체 화면 인터랙션처럼 예외가 필요한 경우 목적과 확인 화면을 PR에 기록한다.

## 2. 정렬과 밀도

- 인접 섹션의 content shell, 왼쪽 기준선, eyebrow 크기와 본문 폭을 먼저 비교한다.
- 한 섹션만 별도 spacing 값을 만들지 않고 `DESIGN.md`의 간격 토큰과 기존 인접 섹션 값을 재사용한다.
- 콘텐츠가 짧다는 이유로 섹션 높이를 억지로 채우지 않는다. 내용에 맞는 높이를 사용한다.
- 색상 장면이 전환될 때 여백을 두 배로 늘려 구분하지 않는다. 배경 전환과 한 번의 표준 여백이면 충분하다.
- 모바일에서는 데스크톱 여백을 단순 유지하지 않고 390px 화면에서 첫 메시지와 다음 행동이 한 화면 안에서 함께 읽히는지 확인한다.

## 3. 구현 전후 검증

1. 변경 섹션과 바로 앞뒤 섹션의 padding, 첫 콘텐츠 좌표를 확인한다.
2. 390px, 768px, 1440px에서 섹션 경계와 첫 콘텐츠가 함께 보이도록 캡처한다.
3. 변경 섹션만 보지 않고 앞 섹션의 마지막 15%와 뒤 섹션의 첫 30%를 포함해 리듬을 비교한다.
4. Playwright로 섹션 시작 여백의 허용 상한과 인접 섹션 간 32px 편차를 검증한다.
5. 가로 overflow, 텍스트 겹침, 의미 없는 빈 영역이 없을 때만 완료로 처리한다.

## 4. PR 필수 기록

- Before / After는 같은 뷰포트와 같은 스크롤 위치를 사용한다.
- UI 변경 설명에 `여백`, `정렬`, `반응형` 확인 결과를 각각 한 줄로 적는다.
- 수치 기준을 넘긴 예외는 이유, 적용 범위, 제거 조건을 명시한다.
- 자동 검증을 추가할 수 없는 시각 변경은 390px과 1440px 수동 확인 결과를 남긴다.

## 5. 완료 조건

- [ ] 섹션 첫 콘텐츠가 뷰포트별 허용 상한 안에 있다.
- [ ] 같은 위계의 인접 섹션 시작 위치 차이가 32px 이하다.
- [ ] 의미 없는 240px 이상의 빈 영역이 없다.
- [ ] 390px, 768px, 1440px 경계 캡처를 확인했다.
- [ ] 관련 Playwright 검사와 기존 품질 명령이 통과했다.

## 6. 완료 보고 게이트

- 자동 테스트와 CSS 수치가 통과해도 실제 화면이 조잡하거나 비어 보이면 완료가 아니다.
- 변경한 기본 상태와 모든 주요 인터랙션 상태를 390px과 1440px에서 직접 확인한다. 선택 영역만 확대해서 보지 않고 화면 안의 앞뒤 콘텐츠까지 함께 본다.
- 각 빈 공간에 정보 위계, 장면 전환, 조작 영역 확보 중 하나의 명확한 목적이 있는지 판단한다. 설명할 목적이 없는 빈 공간은 제거한다.
- 고정 높이, `min-height`, 큰 margin과 padding 때문에 다음 콘텐츠가 화면 밖으로 밀리거나 시각 흐름이 끊기면 결함으로 처리한다.
- 스크린샷에서 발견한 어색함을 "의도된 여백"으로 합리화하지 않는다. 사용자 요구나 문서에 명시된 의도가 아니라면 수정한다.
- 완료 보고 전 `의도 전달`, `밀도`, `정렬`, `반응형`, `다음 행동의 가시성`을 마지막으로 점검한다. 하나라도 부족하면 완료를 보고하지 않고 먼저 수정한다.
