# 커밋 컨벤션

## 기본 형식

```text
<Type> 한국어로 변경 목적을 설명
```

예시:

```text
<Feat> 스튜디오 메인 페이지와 문의 API 추가
<Fix> 모바일 메뉴 스크롤 잠금 오류 수정
<Design> 프로젝트 카드의 타이포그래피와 간격 조정
<Docs> 디자인 토큰 사용 기준 보완
```

## Type

| Type         | 사용 시점                         |
| ------------ | --------------------------------- |
| `<Feat>`     | 사용자에게 보이는 기능 추가       |
| `<Fix>`      | 오류 또는 회귀 수정               |
| `<Design>`   | 기능 변화 없는 시각·인터랙션 수정 |
| `<Refactor>` | 동작을 유지한 코드 구조 개선      |
| `<Docs>`     | 문서만 변경                       |
| `<Test>`     | 테스트 추가 또는 수정             |
| `<Chore>`    | 설정, 패키지, 빌드 작업           |
| `<Perf>`     | 성능 개선                         |

## 작성 원칙

- 제목은 명령형보다 완료한 변경을 명확히 설명한다.
- 한 커밋에는 하나의 목적만 담는다.
- 역할과 기능 단위로 작게 나누고, 독립적으로 되돌릴 수 있는 변경은 별도 커밋으로 만든다.
- 초기 설정, 디자인 문서, 공용 UI, 개별 화면, API, 메타데이터·파비콘, 테스트를 한 커밋에 합치지 않는다.
- `수정`, `작업`처럼 대상이 없는 표현은 피한다.
- 제목은 60자 안쪽을 권장한다.
- 본문이 필요하면 빈 줄 뒤에 변경 이유와 검증 방법을 적는다.

## 승인 규칙

- 커밋을 만들거나 기존 커밋을 수정·분할하기 전에 사용자에게 예정된 커밋 목록을 제시하고 허락을 받는다.
- 구현 요청은 커밋 승인으로 간주하지 않는다.
- 푸시는 커밋 승인과 별개이며, remote·branch·대상 커밋을 알린 뒤 반드시 별도 허락을 받는다.
- force push는 위험을 설명하고 다시 명시적인 허락을 받는다.

## 역할 단위 예시

```text
<Chore> Next.js 프로젝트 기본 환경 구성
<Docs> 디자인 시스템과 반응형 기준 문서화
<Feat> 공통 헤더와 모바일 메뉴 구현
<Feat> 메인 히어로 섹션 구현
<Feat> 문의 API와 입력 검증 구현
<Design> 파비콘과 메타데이터 설정
<Test> 홈페이지 반응형 E2E 테스트 추가
```

## 커밋 전 확인

```bash
npm run lint
npm run typecheck
npm run build
npm run test:e2e
```
