# Jabin 작업 하네스

이 파일의 규칙은 이 저장소에서 수행하는 모든 작업에 적용한다.

## 응답 및 토큰 사용

- 사용자가 이미 제공한 요구사항을 다시 요약하거나 반복하지 않는다.
- 구현과 직접 관계없는 일반론, 디자인 철학, 칭찬, 형식적인 서론과 결론을 작성하지 않는다.
- 디자인 분석은 실제 구현 결정에 영향을 주는 내용만 짧게 작성한다.
- 구현 전 계획은 핵심 3~5개 이내로 제한한다.
- 여러 대안을 장황하게 나열하지 않고 현재 프로젝트에 가장 적절한 하나를 선택해 구현한다.
- 코드 변경 요청은 설명보다 실제 코드 수정에 토큰을 우선 사용한다.
- 기존 컴포넌트와 스타일을 재사용하고 불필요한 구조나 추상화를 추가하지 않는다.
- 최종 보고는 수정 내용, 검증 결과, 남은 위험만 짧게 작성한다.
- 토큰 절약을 이유로 디자인 완성도, 반응형, 접근성, 보안, 테스트와 코드 품질을 생략하지 않는다.

## 디자인 작업

- 프론트엔드 작업 전 `DESIGN.md`, `DESIGN_NAVY.md`, `.agents/skills/design-taste-frontend/SKILL.md`, `.agents/skills/design-taste-frontend/JABIN_PROFILE.md`를 확인한다.
- 레이아웃과 UI를 수정할 때 `VISUAL_QA_HARNESS.md`의 섹션 여백, 인접 섹션 편차, 경계 캡처 기준을 반드시 검증한다.
- 디자인 기준의 우선순위는 사용자 요구사항, `DESIGN.md`, `VISUAL_QA_HARNESS.md`, `DESIGN_NAVY.md`, `JABIN_PROFILE.md`, 범용 `SKILL.md` 순서다.
- 구현 전에 대상 사용자, 시각 방향, `DESIGN_VARIANCE`, `MOTION_INTENSITY`, `VISUAL_DENSITY`를 한 줄의 Design Read로 선언한다.
- 완료 전 `JABIN_PROFILE.md`의 Preflight와 반응형 시각 검증을 수행한다.
- UI 작업은 테스트 통과만으로 완료 처리하지 않는다. 최종 화면을 직접 보고 의도, 정보 밀도, 빈 공간, 인접 콘텐츠의 흐름을 판단한 뒤 결함을 먼저 수정하고 완료를 보고한다.

## 공동 작업

- 작업 전 `COLLABORATION_HARNESS.md`를 읽고 브랜치 흐름, 작업 범위 공유, 인수인계 규칙을 따른다.
- `main`과 `develop`에서 직접 기능을 작업하지 않고 목적별 작업 브랜치를 사용한다.
- 자동화 도구를 포함한 작업자는 수정 전에 현재 브랜치와 기존 변경 파일을 확인한다.
- 자신이 만들지 않은 변경은 공동 작업자의 진행 중 작업으로 간주하며 임의로 되돌리지 않는다.
- 첫 push 뒤 Draft PR을 열고 PR 템플릿의 변경 범위와 인수인계를 최신 상태로 유지한다.
- UI 변경 PR은 Playwright로 base와 head의 같은 뷰포트·같은 DOM 영역을 캡처해 Before / After를 본문에 넣는다. 전체 페이지 캡처는 전역 레이아웃 변경에만 사용한다.
- 열림, 선택, 오류처럼 변경의 핵심이 되는 인터랙션 상태도 해당 컴포넌트 영역만 추가 캡처한다. PR 검토 이미지는 기능 브랜치의 제품 파일에 포함하지 않는다.

## 커밋 단위

- 커밋은 역할과 기능 단위로 작게 나눈다.
- 서로 독립적으로 설명하거나 되돌릴 수 있는 변경은 반드시 별도 커밋으로 분리한다.
- 초기 설정, 디자인 문서, 공용 UI, 개별 섹션, API, 메타데이터, 파비콘, 테스트를 한 커밋에 합치지 않는다.
- 파일 수보다 변경 목적을 기준으로 나눈다. 하나의 파일을 수정하더라도 목적이 다르면 커밋을 분리한다.
- 각 커밋은 가능하면 해당 변경만으로 lint, typecheck 또는 관련 테스트가 통과하는 상태여야 한다.

권장 분할 예시:

```text
<Chore> Next.js 프로젝트 기본 환경 구성
<Docs> 디자인 시스템과 반응형 기준 문서화
<Feat> 공통 헤더와 모바일 메뉴 구현
<Feat> 메인 히어로 섹션 구현
<Feat> 프로젝트 쇼케이스 섹션 구현
<Feat> 문의 API와 입력 검증 구현
<Design> 파비콘과 메타데이터 설정
<Test> 홈페이지 반응형 E2E 테스트 추가
```

## 커밋 승인

- `git commit`, 커밋 수정, 커밋 분할, squash, rebase 등 커밋 이력에 영향을 주는 작업은 실행 전에 반드시 사용자에게 허락을 받는다.
- 사용자가 구현을 요청했다는 사실만으로 커밋까지 승인한 것으로 간주하지 않는다.
- 커밋 전에는 예정된 커밋 목록과 각 커밋에 포함될 변경을 사용자에게 먼저 제시한다.
- 사용자가 명시적으로 승인한 범위와 메시지로만 커밋한다.

## 푸시 승인

- `git push`는 일반 push와 force push를 포함해 항상 실행 전에 사용자에게 별도 허락을 받는다.
- 커밋 승인은 푸시 승인으로 간주하지 않는다.
- 사용자가 "커밋하고 푸시"처럼 두 작업을 함께 명시적으로 승인한 경우에만 연속 실행할 수 있다.
- 푸시 전 대상 remote, branch, 포함될 커밋을 사용자에게 알린다.
- `--force` 또는 `--force-with-lease`가 필요한 경우 일반 푸시와 별도로 위험을 설명하고 다시 승인받는다.

## 커밋 메시지

- 형식은 `<Type> 한국어 설명`을 사용한다.
- Type과 세부 기준은 `COMMIT_CONVENTION.md`를 따른다.
