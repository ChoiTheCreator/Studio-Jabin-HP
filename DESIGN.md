# Jabin Design Guide

이 문서는 디자이너 없이도 화면의 품질과 일관성을 유지하기 위한 단일 기준이다. 새로운 화면을 만들거나 기존 화면을 수정할 때 구현보다 먼저 이 문서를 갱신한다. Tailwind `@theme` 토큰과 이 문서의 값이 다르면 의도된 변경인지 확인하고 둘을 함께 수정한다.

## 1. 브랜드 방향

### 핵심 인상

Jabin은 전략, 디자인, 개발을 따로 판매하는 제작사가 아니라 생각을 실제로 작동시키는 디지털 스튜디오다. 화면은 다음 네 단어를 기준으로 판단한다.

- 선명함: 한 화면에는 하나의 메시지가 가장 먼저 보여야 한다.
- 정밀함: 정렬, 간격, 전환 속도가 임의로 느껴지지 않아야 한다.
- 생동감: 정적인 포트폴리오가 아니라 제작 과정이 움직이는 인상을 준다.
- 절제: 장식보다 실제 작업과 문장이 화면을 이끈다.

### 벤치마크 적용 원칙

Eternal FE에서 가져오는 것은 브랜드 표현이 아니라 다음 시스템이다.

- 첫 화면을 가득 채우는 강한 시각 신호
- 스크롤 위치에 반응해 형태가 바뀌는 헤더
- 큰 제목과 작은 설명 사이의 명확한 대비
- 뷰포트 진입 시 순차적으로 나타나는 섹션
- 모바일과 데스크톱의 미디어·간격·타입 크기 분리

Eternal의 파란색 중심 팔레트, 로고, 문구, 이미지와 구체적인 레이아웃 수치는 복제하지 않는다.

### Taste Skill 적용

- 범용 Taste Skill은 `.agents/skills/design-taste-frontend/SKILL.md`에 프로젝트 로컬로 보관한다.
- 실제 적용 시 `.agents/skills/design-taste-frontend/JABIN_PROFILE.md`를 함께 읽고 이 문서의 확정된 브랜드 기준을 우선한다.
- 현재 기본 다이얼은 `DESIGN_VARIANCE 6`, `MOTION_INTENSITY 4`, `VISUAL_DENSITY 4`다.
- 원본 Skill을 수정해 프로젝트 예외를 섞지 않는다. Jabin 전용 결정은 프로필과 이 문서에 기록한다.

## 2. 디자인 토큰

### 색상

| 역할  | 토큰            | 값                   | 사용                       |
| ----- | --------------- | -------------------- | -------------------------- |
| Ink   | `--color-ink`   | `#11110f`            | 기본 텍스트, 다크 배경     |
| Paper | `--color-paper` | `#f4f2ed`            | 기본 페이지 배경           |
| White | `--color-white` | `#ffffff`            | 밝은 패널, 반전 텍스트     |
| Blue  | `--color-blue`  | `#2447ff`            | 핵심 액션, 링크, 포커스    |
| Red   | `--color-red`   | `#f04a2f`            | 번호, 강조, 보조 시각 신호 |
| Lime  | `--color-lime`  | `#c9ff3d`            | 선택적 하이라이트          |
| Muted | `--color-muted` | `#6f706b`            | 보조 설명                  |
| Line  | `--color-line`  | `rgba(17,17,15,.16)` | 구분선                     |

팔레트는 한 가지 색에 잠기지 않는다. 한 섹션 안에서 강조색은 하나를 우선하고, Blue·Red·Lime을 같은 강도로 동시에 사용하지 않는다. 본문 텍스트는 색상으로 위계를 만들기보다 크기와 굵기를 먼저 사용한다.

### 형광펜 강조

- `src/components/ui/TextHighlight.tsx`의 `TextHighlight`를 사용해 메시지에서 정말 중요한 1개 구절만 강조한다.
- 강조색은 `--color-lime`을 사용하며, 둥근 모서리, 그림자, gradient, 자동 애니메이션을 추가하지 않는다.
- 기본 메시지는 한 문장에 한 번만 사용한다. 인프라 disclosure처럼 두 거점의 제공 범위를 비교하는 경우에는 지역별 핵심 서비스 키워드를 최대 3개까지 강조할 수 있다.
- 형광펜은 위계 보조 수단이며 제목 크기와 여백을 대체하지 않는다. 본문에서 사용할 때는 강조어만 Ink 색상과 Bold로 전환한다.

### 타이포그래피

기본 서체는 Freesentation이다. 로컬 WOFF2 파일을 사용해 네트워크 상태와 무관하게 동일하게 보이도록 한다.

| 스타일  | Desktop                      | Mobile                    | 굵기 | 행간 |
| ------- | ---------------------------- | ------------------------- | ---- | ---- |
| Hero    | `112px` (`1440px+`: `132px`) | `48px` (`640px+`: `78px`) | 700  | 0.92 |
| Display | `108px` (`1440px+`: `118px`) | `48px` (`640px+`: `72px`) | 700  | 0.95 |
| H2      | `68–92px`                    | `42–50px`                 | 700  | 0.98 |
| H3      | `1.75rem`                    | `1.35rem`                 | 700  | 1.1  |
| Lead    | `1.25rem`                    | `1.05rem`                 | 500  | 1.55 |
| Body    | `1rem`                       | `0.95rem`                 | 500  | 1.65 |
| Label   | `0.75rem`                    | `0.7rem`                  | 700  | 1.2  |

- 글자 간격은 `0`을 기본으로 한다.
- 영문 대문자 라벨만 `0.04em`까지 허용한다.
- 글자 크기는 브레이크포인트별 고정값으로 관리한다. 뷰포트 너비에 비례해 계속 변하는 `vw` 타입은 사용하지 않는다.
- 한글은 의미 단위로 줄바꿈한다. 모바일에서 조사 한 글자만 다음 줄에 남지 않게 확인한다.
- 긴 영문은 `overflow-wrap: anywhere`를 허용한다.

### 간격

4px 배수를 기본으로 하되 화면의 큰 리듬은 아래 토큰을 사용한다.

```text
4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128 / 160
```

- 페이지 좌우 여백: 모바일 20px, 태블릿 32px, 데스크톱 48px
- 콘텐츠 최대 폭: 1440px
- 본문 읽기 폭: 680px
- 섹션 상하 여백: 모바일 88px, 데스크톱 144px
- 제목과 설명 사이: 모바일 24px, 데스크톱 32px
- 반복 항목 간격: 모바일 16px, 데스크톱 24px

### 모서리와 선

- 일반 카드: 0~8px. 반복 카드만 경계를 가진다.
- 이미지: 6px. 이미지 비율 자체가 주인공이 되도록 과도하게 둥글리지 않는다.
- 플로팅 헤더: 완전한 pill 형태 허용.
- 버튼: 999px pill은 명확한 CTA에만 사용한다.
- 구분선: 1px, Tailwind `border-line` 사용.
- 섹션 전체를 떠 있는 카드처럼 만들지 않는다.

### 그림자

기본 화면은 선과 면으로 구분한다. 그림자는 플로팅 헤더와 모달에만 사용한다.

플로팅 헤더는 `shadow-[0_16px_48px_rgba(17,17,15,0.14)]` 값을 사용한다.

## 3. 레이아웃 시스템

### 공통 컨테이너

`src/components/ui/tailwind.ts`의 `pageShell` 클래스 상수는 `max-width: 1440px`와 반응형 좌우 여백을 가진다. 섹션 배경은 전체 너비를 사용하고, 내부 콘텐츠만 컨테이너에 맞춘다.

### 그리드

- 데스크톱: 12열 개념, 실제 Tailwind grid utility에서는 필요한 열만 선언한다.
- 태블릿: 8열 개념.
- 모바일: 4열 개념 또는 한 열.
- 프로젝트 목록은 데스크톱 12열에서 7:5 비율 교차 배치, 모바일에서는 한 열이다.
- 고정 비율 미디어는 `aspect-ratio`를 반드시 지정해 로딩 전후 이동을 방지한다.

### 브레이크포인트

| 이름    | 범위         | 주요 변화                           |
| ------- | ------------ | ----------------------------------- |
| Mobile  | `< 640px`    | 한 열, 메뉴 드로어, 간격 축소       |
| Tablet  | `640–1023px` | 2열 일부 허용, 타입 중간값          |
| Desktop | `≥ 1024px`   | 가로 내비게이션, 프로젝트 교차 배치 |
| Wide    | `≥ 1440px`   | 콘텐츠 폭 고정, 여백만 증가         |

브레이크포인트는 특정 기기명이 아니라 콘텐츠가 깨지는 지점을 기준으로 추가한다.

## 4. 홈페이지 섹션 명세

### Header

- 초기: 화면 상단 전체 폭, 흰색 로고와 내비게이션.
- 48px 이상 스크롤: 중앙 pill, 흰색 반투명 배경, 검정 텍스트.
- 변환 시간: 550ms, `cubic-bezier(.22,1,.36,1)`.
- 모바일: 로고, 문의 버튼, 메뉴 아이콘만 노출.
- 메뉴가 열리면 body 스크롤을 잠그고 Escape 키와 바깥 영역으로 닫을 수 있어야 한다.

### Hero

- 높이: 모바일·데스크톱 모두 `100svh`. 다음 섹션의 색상 띠가 보이도록 내부 콘텐츠는 하단에 여유를 둔다.
- 배경: 실제 스튜디오/작업 이미지를 full bleed로 사용한다.
- 이미지는 텍스트 가독성을 위해 검정 오버레이를 사용하되 흐리게 만들지 않는다.
- 브랜드명, 핵심 문장, 서비스 범위가 첫 화면 안에 보여야 한다.
- 페이지 본문은 500ms 동안 opacity 0과 scale 0.985에서 최종 상태로 진입한다.
- 배경 이미지는 로드 완료 전 opacity 0과 scale 1.08을 유지하고, 완료 후 1200ms 동안 opacity 1과 scale 1.025로 정착한다.
- 헤더는 120ms 지연 후 700ms 동안 위에서 진입한다.
- 초기 진입은 label 120ms → headline 각 줄 240/380/520ms → meta 700ms 순서로 나타난다.
- 헤드라인 각 줄은 overflow mask 안에서 아래에서 위로 950ms 동안 reveal한다.
- 포인터 이동 패럴랙스는 데스크톱 fine pointer에서만 최대 12px 사용한다.

### Project Showcase

- 섹션 시작에서 `Selected work`와 프로젝트 수를 함께 제공한다.
- 각 프로젝트는 번호, 이름, 분야, 연도와 결과 이미지를 가진다.
- hover 가능한 환경에서만 이미지 1.03배 확대와 화살표 이동을 사용한다.
- 모바일 터치에는 hover 상태를 강제하지 않는다.
- 실제 프로젝트 자료가 확보되면 `home.content.ts`의 임시 항목과 이미지를 먼저 교체한다.

### Engineering & Operations

- Blue 전체 폭 배경을 사용해 포트폴리오와 서비스 목록 사이의 기술적 전환점을 만든다.
- 기술 로고를 나열하지 않고 `Users → Web/App → API → Data → Cloud → Monitoring` 흐름을 먼저 보여준다.
- 시스템 노드는 모바일에서 세로, 태블릿에서 2열, 데스크톱에서 6열로 재배치한다.
- 노드 사이 화살표는 정보의 방향만 설명하며 클릭 요소처럼 보이지 않게 한다.
- 운영 역량은 Application, Infrastructure, Reliability, Security 네 축으로 고정한다.
- 실제로 검증되지 않은 기술명, 가동률, 응답 시간은 노출하지 않는다.
- reveal 애니메이션은 시스템 흐름 순서대로 90ms 간격을 사용하고 reduced motion에서는 즉시 표시한다.

### Owned Infrastructure

- Engineering & Operations 다음에 배치해 운영 역량을 실제 보유 자원으로 증명한다.
- 첫 인상은 `02 REGIONS`, `Seoul ↔ Gwangju`, 자체 운영 메시지로 제한한다. 서버 부품명은 기본 화면에 노출하지 않는다.
- 서울은 AI Compute, 광주는 Core Compute로 역할을 분리하되 두 거점을 우열 관계로 표현하지 않는다.
- 두 지역 사이의 긴 여백과 1px 연결선을 핵심 시각 요소로 사용한다. 연결 신호 외의 노드, 아이콘, 지도 그래픽은 만들지 않는다.
- 서버랙 사진이나 콘솔 화면을 사용하지 않고 큰 타이포그래피, 여백, 정렬, 얇은 선으로 구성한다.
- 서버 사양은 영업 판단에 필요한 수준만 공개한다. 자산 식별 정보, IP, 네트워크 구성, 상세 보안 설정은 노출하지 않는다.
- GPU는 `20GB per GPU`, `40GB Total`로 표기하며 하나의 40GB 통합 메모리처럼 표현하지 않는다.
- 상세 사양은 네이티브 disclosure가 열린 뒤에만 보이며 카드가 아닌 typography list로 표현한다.
- disclosure 헤더는 제목과 한 줄 목적 설명으로 정보 위계를 만들고, 우측의 1px 라인 인디케이터가 `+`에서 `−`로 전환되어 열린 상태를 보여준다.
- 상세 내용은 열릴 때 opacity와 12px 이동만 사용해 진입하며, reduced motion에서는 애니메이션 없이 즉시 표시한다.
- disclosure 안에서는 각 거점이 제공할 수 있는 서비스 문장을 사양보다 먼저 배치해, 비개발자도 하드웨어의 용도를 이해할 수 있게 한다.
- Cloud와 Owned Infrastructure는 대체 관계가 아니라 baseline과 peak 요구사항에 따라 조합하는 선택지로 설명한다.
- 지역 연결 신호는 8초 동안 천천히 이동하고 reduced motion에서는 숨긴다.
- 모바일에서도 `Seoul ↔ Gwangju` 관계를 가로로 유지하되 타입을 축소하고 상세 사양은 한 열로 전환한다.
- CTA는 별도 상세 페이지가 생기기 전까지 문의 섹션으로 연결한다.

### Capabilities

- 기능을 카드 모음처럼 보이지 않게 수평 구분선 목록으로 표현한다.
- 항목마다 번호, 서비스명, 짧은 범위를 제공한다.
- 한 항목의 설명은 두 줄 이내를 권장한다.

### Team

- 조직도보다 실제 프로젝트를 책임지는 구성원의 이름, 역할, 책임 범위를 우선한다.
- 구성원은 동일한 정보 순서로 표시한다: 번호 → 직책 → 이름 → 책임 → 전문 영역 → 외부 링크.
- 사진이 없는 초기 상태에서는 임의의 인물 이미지 대신 영문 이니셜 블록을 사용한다.
- LinkedIn, GitHub, Portfolio 세 링크 슬롯을 구성원마다 동일하게 유지한다.
- URL이 확인되지 않은 링크는 `#`으로 연결하지 않고 비활성 상태로 표시한다.
- 전체 인원과 공개 프로필 수가 다르면 두 수치를 구분해 표시한다.
- 모바일은 한 구성원의 모든 정보를 읽은 뒤 다음 구성원으로 이동하는 한 열 흐름을 사용한다.
- 실제 사진을 추가할 때는 동일한 비율, 촬영 톤, 배경 처리 기준을 세 구성원 모두에게 적용한다.

### Process

- Diagnose, Scope, Build, Launch, Operate 다섯 단계로 고정한다.
- 데스크톱은 5열 가로 진행, 모바일은 세로 진행, 태블릿은 2열이며 마지막 단계를 전체 폭으로 표시한다.
- 단계명보다 실제 산출물이 무엇인지 설명한다.

### Inquiry

- Blue 전체 폭 배경으로 페이지의 종결감과 명확한 전환을 만든다.
- 제목과 실제 문의 폼을 같은 화면 흐름에 둔다.
- 필수 항목은 이름, 이메일, 프로젝트 유형, 필요한 범위, 프로젝트 설명, 개인정보 동의다.
- 회사/브랜드, 연락처, 예상 일정, 예산 구간은 선택 항목으로 둔다.
- 필요한 범위는 checkbox, 유형·일정·예산은 select를 사용해 입력 형식을 고정한다.
- select와 checkbox는 실제 form control을 유지하고 키보드와 스크린 리더로 조작할 수 있어야 한다.
- 제출 상태는 idle, submitting, success, error 네 가지다.
- 성공/오류 메시지는 색상뿐 아니라 텍스트로 명확히 표현한다.
- 필드 오류는 해당 control의 `aria-describedby`와 연결한다.
- 개인정보 동의 문구에서 `/privacy` 안내 페이지로 이동할 수 있어야 한다.

## 5. 모션 시스템

### 원칙

모션은 요소를 꾸미는 장식이 아니라 읽는 순서를 만든다. 한 화면에서 서로 다른 easing을 과도하게 섞지 않는다.

### 기본 값

| 이름         | 시간  | easing                      | 사용                    |
| ------------ | ----- | --------------------------- | ----------------------- |
| Fast         | 180ms | ease-out                    | 아이콘, 색상, 눌림      |
| Base         | 320ms | ease-out                    | 버튼, 이미지 hover      |
| Page Enter   | 500ms | `cubic-bezier(.22,1,.36,1)` | 페이지 본문 진입        |
| Header Enter | 700ms | `cubic-bezier(.22,1,.36,1)` | 첫 로드 헤더 진입       |
| Hero Line    | 950ms | `cubic-bezier(.22,1,.36,1)` | 히어로 줄별 mask reveal |
| Reveal       | 800ms | `cubic-bezier(.22,1,.36,1)` | 스크롤 진입             |
| Transform    | 550ms | `cubic-bezier(.22,1,.36,1)` | 헤더 형태 변경          |

### Reveal

- 시작: opacity 0, translateY 36px.
- 종료: opacity 1, translateY 0.
- 한 그룹 안의 delay는 80~120ms씩 증가하고 총 500ms를 넘기지 않는다.
- 뷰포트 12%가 보이거나 하단 8% 이전에 들어오면 한 번 실행한다.
- 애니메이션 전에도 스크린 리더와 검색엔진에는 콘텐츠가 존재해야 한다.

### 지속 모션

- 마키는 28~36초 선형 반복.
- 스크롤 힌트는 1.6초 반복.
- 동시에 반복되는 모션은 한 화면에 두 종류를 넘기지 않는다.
- 페이지가 백그라운드일 때 브라우저가 자연스럽게 중단할 수 있는 CSS animation을 우선한다.

### 모션 접근성

`prefers-reduced-motion: reduce`에서 다음을 지킨다.

- 모든 reveal을 즉시 최종 상태로 표시한다.
- 마키와 스크롤 반복 애니메이션을 멈춘다.
- 패럴랙스 값을 0으로 유지한다.
- 상태 이해에 필요한 색상 변화는 유지하되 이동·확대는 제거한다.

## 6. 인터랙션과 접근성

- 모든 클릭 가능한 요소는 키보드로 접근 가능해야 한다.
- 포커스 링은 2px Blue, 3px offset을 사용한다.
- 아이콘만 있는 버튼은 `aria-label`을 제공한다.
- 터치 영역은 최소 44×44px이다.
- 본문 대비는 WCAG AA 이상을 목표로 한다.
- 폼 label은 placeholder로 대체하지 않는다.
- 오류는 해당 필드와 제출 결과 영역에서 이해할 수 있어야 한다.
- 이미지에는 목적에 맞는 대체 텍스트를 작성하고 장식 이미지는 빈 alt를 사용한다.

## 7. 이미지 운영

- 히어로: 16:9 기준, 최소 1920×1080 권장.
- 프로젝트 이미지: 4:3 또는 3:2 중 프로젝트 내부에서 하나로 통일.
- `next/image`를 사용하고 실제 렌더링 폭에 맞는 `sizes`를 지정한다.
- LCP 대상 히어로만 priority로 불러온다.
- 텍스트가 포함된 이미지는 가능하면 피하고 실제 텍스트를 HTML로 제공한다.
- AI 생성 이미지는 임시/콘셉트 상태임을 콘텐츠 목록에서 관리하고 실제 프로젝트 이미지 확보 시 교체한다.

브랜드 이미지는 역할별 폴더로 관리한다.

```text
public/images/
├── brand/                     # 서비스에 사용하는 Jabin 로고 파생본
│   ├── source/                # 전달받은 원본, 직접 수정 금지
│   ├── jabin-logo-lockup.png  # 심볼과 워드마크 세로 조합
│   ├── jabin-logo-word.png    # 헤더용 워드마크
│   ├── jabin-logo-mark.png    # 단독 심볼
│   ├── jabin-favicon.png      # 정사각형 파비콘
│   └── jabin-social-preview.png # 링크 공유용 1200×630 대표 이미지
└── work/                      # 프로젝트와 콘셉트 이미지
    └── jabin-studio-hero.png
```

현재 `jabin-studio-hero.png`는 imagegen으로 생성한 초기 콘셉트 에셋이다. 실제 스튜디오 또는 프로젝트 촬영본을 확보하면 같은 파일명으로 덮지 말고 새 파일을 추가한 뒤 콘텐츠 참조를 변경해 변경 이력을 남긴다.

링크 공유 대표 이미지는 `jabin-social-preview.png`를 사용한다. Open Graph와 X/Twitter 모두 1200×630 크기의 같은 파일을 참조하며, 작은 미리보기에서도 식별할 수 있도록 Navy 단색 배경과 흰색 로고만 사용한다.

## 8. 컴포넌트 규칙

- 섹션 컴포넌트는 자기 레이아웃만 책임지고 전역 header/footer 상태를 직접 변경하지 않는다.
- 반복 데이터는 JSX에 복제하지 않고 `home.content.ts`에 둔다.
- 두 화면 이상에서 쓰이는 패턴만 공용 컴포넌트로 승격한다.
- variant가 둘뿐이라면 거대한 추상화보다 명시적인 className을 우선한다.
- 고정 높이 안의 동적 텍스트는 overflow 여부를 모바일과 데스크톱에서 확인한다.

## 9. 문구 원칙

- 추상적인 수식어보다 무엇을 만들고 어떤 결과를 주는지 쓴다.
- 헤드라인은 두 문장 이내, 보조 설명은 세 줄 이내다.
- `최고`, `혁신`, `압도적`처럼 증명하기 어려운 표현은 사용하지 않는다.
- 영문은 섹션 라벨과 프로젝트 고유명에만 제한적으로 사용한다.
- CTA는 행동 결과가 보이도록 `문의하기`, `프로젝트 시작하기`처럼 쓴다.

## 10. 변경 절차

1. 변경하려는 화면과 관련된 이 문서 항목을 읽는다.
2. 새 규칙이 필요하면 디자인 문서를 먼저 수정한다.
3. Tailwind `@theme` 토큰 또는 컴포넌트 유틸리티를 수정한다.
4. 390×844, 768×1024, 1440×900에서 확인한다.
5. 키보드 탐색과 reduced motion을 확인한다.
6. 디자인 변경이면 `<Design> 한국어 설명`, 기능까지 바뀌면 `<Feat> 한국어 설명`으로 커밋한다.

## 11. 출시 전 체크리스트

- 첫 화면에서 Jabin의 이름과 제공 가치가 보이는가
- 각 섹션의 첫 시선이 하나로 정리되는가
- 실제 프로젝트가 임시 이미지보다 먼저 보이는가
- 긴 한글 문장이 모바일에서 잘리지 않는가
- 헤더가 스크롤 전후 모두 배경과 충분한 대비를 갖는가
- 메뉴, 링크, 폼을 키보드만으로 사용할 수 있는가
- 애니메이션을 껐을 때도 콘텐츠가 모두 보이는가
- 이미지 로딩 전후 레이아웃 이동이 없는가
- 문의 성공과 실패 상태가 구분되는가
- `npm run lint`, `npm run typecheck`, `npm run build`가 통과하는가
