# Jabin 공동 작업 하네스

이 문서는 공동 CTO와 자동화 도구가 같은 저장소에서 작업할 때 사용하는 협업 기준이다. 목표는 Git에 이미 남는 변경 이력을 다시 적는 것이 아니라, 작업의 범위와 이유, 현재 상태, 다음 행동을 PR에서 바로 파악할 수 있게 만드는 것이다.

## 1. 기록의 기준

| 정보 | 기록 위치 |
| --- | --- |
| 무엇을 바꿨는가 | 역할 단위 커밋 |
| 왜 바꿨는가 | PR의 작업 배경과 결정 사항 |
| 누가 작업 중인가 | GitHub 담당자와 Draft PR |
| 지금 어디까지 됐는가 | PR의 인수인계 섹션 |
| 화면 기준은 무엇인가 | `DESIGN.md`, 필요 시 `DESIGN_NAVY.md` |
| 배포 가능한 상태인가 | PR 검증 결과와 CI |

별도의 통합 `WORKLOG.md`는 만들지 않는다. 두 사람이 같은 로그 파일 끝을 계속 수정하면 충돌이 생기고, Git과 PR에 동일한 내용을 중복해서 관리하게 된다.

## 2. 브랜치 흐름

- `main`: 배포 가능한 기준 브랜치. 기능을 직접 작업하지 않는다.
- `develop`: 다음 배포를 모으는 통합 브랜치.
- 작업 브랜치: `develop`에서 분기하고 PR도 `develop`을 대상으로 연다.
- 배포 PR: 검증된 `develop`을 `main`으로 병합한다.

브랜치 이름은 `<type>/<scope>-<slug>` 형식을 사용한다.

```text
feat/home-project-filter
fix/inquiry-email-validation
design/team-mobile-layout
docs/collaboration-harness
```

한 작업 브랜치에는 한 명의 작업 책임자를 둔다. 공동 구현이 필요해도 같은 브랜치에서 동시에 force push하지 않고, 한 명이 push한 뒤 다른 사람이 review 또는 후속 commit을 맡는다.

## 3. 작업 시작 절차

1. `git fetch origin --prune`으로 원격 상태를 갱신한다.
2. `git status --short --branch`로 현재 브랜치와 미완료 변경을 확인한다.
3. `AGENTS.md`, 이 문서, 변경 화면의 `DESIGN.md` 항목을 읽는다.
4. `origin/develop`에서 작업 브랜치를 만든다.
5. 첫 push 뒤 Draft PR을 열고 담당자, 변경 범위, 동시에 수정할 파일을 적는다.
6. 다른 작업자의 Draft PR과 수정 파일이 겹치면 구현 전에 범위를 조정한다.

명령 예시:

```bash
git fetch origin --prune
git switch develop
git pull --ff-only origin develop
git switch -c feat/home-project-filter
```

이미 존재하는 미추적 파일이나 수정 사항은 다른 작업자의 진행 중 작업으로 간주한다. 출처를 확인하기 전에는 삭제, 복원, stash, 덮어쓰기를 하지 않는다.

## 4. 작업 중 공유 규칙

- 커밋은 `AGENTS.md`와 `COMMIT_CONVENTION.md`에 따라 역할 단위로 나눈다.
- 파일 목록만 공유하지 말고 사용자 동작이나 API 계약이 어떻게 바뀌는지 적는다.
- 디자인 변경은 관련 `DESIGN.md` 항목, 사용한 토큰, 확인한 뷰포트를 PR에 남긴다.
- API나 백엔드 변경은 요청/응답, 환경 변수, 저장 방식, 실패 동작을 PR에 남긴다.
- 외부 의존성, 마이그레이션, 배포 순서가 생기면 구현과 동시에 PR의 위험 요소에 기록한다.
- 작업 범위가 커지면 같은 PR에 계속 추가하지 말고 독립적으로 배포하거나 되돌릴 수 있는 브랜치로 분리한다.

## 5. 인수인계 규칙

작업을 넘기거나 그날 작업을 끝낼 때 PR의 `인수인계`를 최신 상태로 만든다.

- 완료: 실제로 끝난 동작과 커밋.
- 진행 중: 미완성 상태와 중단 지점.
- 다음 작업: 다음 사람이 바로 실행할 수 있는 구체적인 한 단계.
- 주의 사항: 임시값, 알려진 오류, 충돌 가능 파일, 필요한 권한.
- 검증: 실행한 명령과 수동 확인한 화면.

Git 작성자 정보가 작업자를 증명하므로 커밋 메시지에 사람 이름을 반복해서 넣지 않는다. 대신 공동 작성한 커밋은 필요할 때 commit trailer를 사용한다.

```text
Co-authored-by: 이름 <email@example.com>
```

## 6. 충돌 처리

1. 충돌이 난 파일의 각 변경 목적을 두 PR에서 먼저 확인한다.
2. 더 먼저 병합된 PR을 기준으로 후속 브랜치를 `develop`에 맞춘다.
3. 생성 파일이나 포맷 결과가 아니라 소유권이 있는 원본 파일에서 충돌을 해결한다.
4. 기능 동작이 겹치면 코드를 합치기 전에 최종 사용자 동작을 합의한다.
5. rebase, squash, force push는 `AGENTS.md`의 승인 규칙을 따른다.

## 7. PR 완료 기준

- PR 템플릿의 작업 배경, 변경 범위, 인수인계가 채워져 있다.
- 관련 디자인 기준 또는 API 계약이 함께 갱신돼 있다.
- `npm run lint`, `npm run typecheck`, `npm run build`가 통과한다.
- 사용자 흐름 변경 시 관련 E2E 테스트 또는 수동 검증 결과가 있다.
- 모바일 390px, 태블릿 768px, 데스크톱 1440px에서 필요한 화면을 확인했다.
- 새 환경 변수, 마이그레이션, 배포 순서가 문서화돼 있다.
- 리뷰어가 집중해서 볼 항목이 명시돼 있다.

## 8. 자동화 도구 규칙

Codex를 포함한 자동화 도구는 작업 시작 시 브랜치, 변경 파일, 관련 문서를 먼저 확인한다. 도구가 만들지 않은 변경은 공동 작업자의 변경으로 취급하며 되돌리지 않는다. 작업 종료 시 수정 파일, 검증 결과, 남은 위험을 보고하고, 커밋과 push는 `AGENTS.md`의 사용자 승인 절차를 따른다.
